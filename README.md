### 1. Run the `concurrency_test.py` to get the best request limit number.
### 2. Run the `main.py` to crawl data from the Tiki API.
### 3. Use the `DataVisualization.ipynb` to explore and clean the crawled data.
